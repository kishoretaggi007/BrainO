<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.co.mirrortechnologies.demogame">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Launching"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Nxt"
            android:label="@string/app_name" />
        <activity
            android:name=".AppOnOpen"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:theme="@style/AppTheme"
            android:name=".TabSlideActivity"
            android:label="BrainO" />

        <activity android:name=".Tab1"
            android:label="BrainO" />
        <activity android:name=".Tab2"
            android:label="BrainO" />
        <activity android:name=".Tab3"
            android:label="BrainO" />
        <activity android:name=".Overall"/>
        <activity android:name=".People"/>
    </application>

</manifest>